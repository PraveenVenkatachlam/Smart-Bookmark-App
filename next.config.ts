import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;


// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   // No special config needed for Supabase + Vercel.
//   // Add image domains here if you plan to show user avatars:
//   // images: {
//   //   remotePatterns: [
//   //     { protocol: "https", hostname: "lh3.googleusercontent.com" },
//   //   ],
//   // },
// };

// export default nextConfig;